import React, { memo } from "react";

import "./Activities.scss";

const ActivitesScreen = () => {

    return (
        <div className="">
           <p>ActivitesScreen</p>
        </div>
    );
};

export default memo(ActivitesScreen);
