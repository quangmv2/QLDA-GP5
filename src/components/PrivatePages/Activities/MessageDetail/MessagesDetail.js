import React, { memo } from "react";

import './MessageDetail.scss';

const MessagesDetail = props => {
    return (
        <div className=''>
            <p>MessagesDetail</p>
        </div>
    )
}

export default memo(MessagesDetail);
