import React, { memo } from "react";
import './Mess.scss';

const MessagesComponent = props => {
    return (
        <div>
            <p>MessagesComponent</p>
        </div>
    )

};

export default memo(MessagesComponent);